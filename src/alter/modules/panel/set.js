/**
 * Controlador do "alter/panel"
 */

// serve pra pre-carregar os tokens
const style = require('./modules/style-loader');
const { adm } = require('./modules/admin-loader');

module.exports = { style, adm };